import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    height: 50,
    borderRadius: 25,
    borderWidth: 1,
    overflow: 'hidden',
  },
  touchContainer: {
    flexGrow: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },
  title: {
    fontSize: 17,
    letterSpacing: 0.5,
  },
})
